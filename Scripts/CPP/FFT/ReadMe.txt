M.a.j 01/03/2015

*On a écrit un programme permettant de calculer la FFT à partir de la méthode des trapèzes dans le modèle de black scholes.

*Il faut vérifier dans le fichier "fonctions.cpp" que T et t (de la classe BS) sont bien différents. 

*Il faut entrer des valeurs initiales données par m. Alfonsi par exemple. Compiler sur ma machine et observer les résultats (s'il y en a). 

*Il faut vérifier l'usage des doubles plutôt que des floats. Si indifférent faire un template.


M.a.j 27/02/2015

*Calcul de la transformée de fourier discrète grâce à la librairie FFTW disponible sur le site (http://www.fftw.org/) on y trouvera également la documentation.

*L'installation sous windows est plutôt douloureuse mais a été réalisée.

*Un premier script pour calculer la transformée de fourier d'un dirac a été réalisé et donne des résultats corrects. Pour les détails du script voir la documentation.
